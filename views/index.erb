<h1>Band/Venue Tracker</h1>


<a href="/bands">Click here to add a band</a>
<br>
<a href="/venues">Click here to add a venue</a>

<h3>Band list:</h3>

<% if @bands.any?() %>
  <p>Click on a band to see the list of venues</p>
  <ul>
    <% @bands.each do |band| %>
      <li><a href="/bands/<%= band.id() %>"><%= band.name() %></a><li>
    <% end %>
  </ul>
<% else %>
  <p>There are no bands yet</p>
<% end %>

<h3>Venue list:</h3>

<% if @venues.any?() %>
  <p>Click on a venue to see the bands that have played there</p>
    <ul>
      <% @venues.each do |venue| %>
      <li><a href="/venues/<%= venue.id() %>"><%= venue.name() %></a><?li>
      <% end %>
    </ul>
<% else %>
  <p>There are no venues yet</p>
<% end %>
