<h1>Band/Venue Tracker</h1>


<p>Add a venue:</p>

<form action="/venues" method="post">
  <label for="venue">Venue Name:</label>
  <input id="venue" name="venue" type="text">
  <br>
  <button type="submit">Add the venue</button>
</form>

<br>

<% if @venues.any?() %>
  <p>Here are the list of venues</p>

  <ul>
    <% @venues.each do |venue| %>
      <li><%= venue.name() %></li>
    <% end %>
  </ul>
<% else %>
<br>
  <p>There are no venues added yet</p>
<% end %>
