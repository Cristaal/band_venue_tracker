Written using Ruby 2.1.5p273

Run '$ gem install bundler'
Run '$ gem install rspec'
Run '$ gem install pry'

Launch Sinatra app with '$ ruby app.rb'

Go to https://local:host4567/

GPL v2 Copyright 2015 Crystal Contreras-Grossman

Known bugs:
Function to add venues to band names is not built yet. There is also no function yet to allow the user to list out band names on the venue view. In it's current state, the user is only able to add an list bands and venues. 
